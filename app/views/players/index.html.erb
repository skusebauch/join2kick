<div class="container">


  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <div id="players">
        <% @players.each do |player| %>
          <h4><%= player.name %></h4>
          <p><%= player.position %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>
