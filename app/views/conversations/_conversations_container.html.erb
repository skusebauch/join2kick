<div class="chatrooms-container" style="width: 20%">
<!-- data-chatroom-name="<%#= @chatrooms.name %>" -->
  <% @chatrooms.each do |chatroom| %>
    <%# @chatroom_already_displayed = false %>
    <%# chatroom.messages.each do |message| %>
    <%# if message.user_id != current_user.id && @chatroom_already_displayed == false%>
    <%# if message.user_id != current_user.id%>
       <%# @chatroom_already_displayed = true %>
       <div class="chatroom-card" data-chatroom-id="<%= chatroom.id %>" style="padding: 10px">
       <%#= link_to chatroom.name, '#', :onclick => 'initChatroomCable(4)' %>
      <%#= link_to message.user.player.name, chatroom_path(chatroom) %>
      <%= link_to chatroom.messages.first.user.player.name , chatroom_path(chatroom) %>
<!--       <button id="chatroom-button" data-chatroom-id="<%= chatroom.id %>">
        <%#= chatroom.name %>
      </button> -->
      </div>
      <%# end %>
    <%# end %>
  <% end %>
</div>
